[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build && pip3 install --user -r backend/requirements.txt"

[deploy]
startCommand = "python3 backend/api_server.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3